# W6 ablation: cell 없음(전역 1개), label 없음 -> block-uniform 주입.
# 주의: 실제로 파싱되는 필드는 method/ablation뿐 — cell_axes/use_label/suffix는
# src/augment/build.py의 ABLATIONS[ablation] 참조용 주석(고쳐도 실행에 반영 안 됨).
method: group_aware
ablation: uniform
cell_axes: []
use_label: false
suffix: "_uniform"
